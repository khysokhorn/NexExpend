<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".NexExpendApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NexExpend">

        <!-- Main app activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NexExpend"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Truly invisible trampoline: starts service and finishes instantly.
             Uses Theme.Trampoline which completely suppresses entrance/exit animations.
             Uses singleInstance and custom taskAffinity so it never pulls the main app's task to the foreground. -->
        <activity
            android:name=".QuickExpenseTrampolineActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleInstance"
            android:noHistory="true"
            android:taskAffinity="com.nextgen.expend.trampoline"
            android:theme="@style/Theme.Trampoline">
            <intent-filter>
                <action android:name="com.nextgen.expend.ACTION_QUICK_EXPENSE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Overlay service: shows the popup on top of any app -->
        <service
            android:name=".QuickExpenseOverlayService"
            android:exported="false" />

        <!--
            Quick Settings Tile — the CORRECT way to trigger the overlay while another app is open.

            Unlike app shortcuts (which launch an Activity and always pause the foreground app),
            TileService.onClick() runs inside a Service. No Activity is launched, so apps like
            Facebook are never paused or sent to the home screen. The overlay appears directly
            on top of them.

            User setup: Pull down twice → tap edit (pencil) → drag "Add Expense" into active tiles.
        -->
        <service
            android:name=".QuickExpenseTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_add_expense"
            android:label="@string/quick_expense_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".network.service.NotificationInterceptorService"
            android:exported="true"
            android:label="Payment Notification Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">

            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>

        </service>

    </application>

</manifest>